#include <conio.h>
#include <stdio.h>
int main()
{
    int f1, f2, sum;
        
	    printf("Enter two integers: ");
    scanf("%d %d", &f1, &f2);

{    sum = f1 + f2;

    printf("%d + %d = %d", f1, f2, sum);

    return 0;
}}
	   
